import SwiftUI

/// WeatherUI provides SwiftUI components and views for the weather application.
/// This module depends on WeatherKit for business logic.
public enum WeatherUI {
    public static let version = "1.0.0"
}
